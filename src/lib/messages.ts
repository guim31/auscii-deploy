/** Messages shared between the jobs and the UI, which reacts to them. */

/** Error of the server step when a paid order needs an admin: the UI then offers the confirmation. */
export const SERVER_ORDER_CONFIRMATION_REQUIRED =
  "Aucun serveur n'a de place disponible. Un administrateur doit confirmer la commande d'un nouveau serveur.";
