services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: auscii
      POSTGRES_PASSWORD: auscii
      POSTGRES_DB: auscii
    ports:
      - "5432:5432"
    volumes:
      - db-data:/var/lib/postgresql/data

volumes:
  db-data:
